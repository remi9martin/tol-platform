{
  "name": "@tol/testkit",
  "version": "0.0.0",
  "private": true,
  "description": "Fixtures, factories, fake connectors for unit/contract/integration/E2E tests (the spec names 16 fixtures). Reference seed: the prototype's lib/seed.ts *shapes* (not values) — restructure to the new decomposed entities, don't import wholesale.",
  "type": "module",
  "scripts": {
    "build": "echo \"@tol/testkit: no build defined yet (earlier skeleton)\"",
    "lint": "echo \"@tol/testkit: no lint defined yet (earlier skeleton)\"",
    "test": "echo \"@tol/testkit: no tests defined yet (earlier skeleton)\""
  },
  "main": "./src/index.ts",
  "types": "./src/index.ts"
}
