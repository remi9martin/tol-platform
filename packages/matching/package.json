{
  "name": "@tol/matching",
  "version": "0.0.0",
  "private": true,
  "description": "Eligibility (hard-rule determinism, the spec) then ranking (explainable, versioned factors, the spec). One of the four business-invariant packages named on the spec. Deterministic-only for MVP (see ADR-0004).",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src",
    "test": "vitest run"
  },
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "devDependencies": {
    "@types/node": "24.13.3",
    "typescript": "^5.9.3",
    "vitest": "^3.2.7"
  }
}
