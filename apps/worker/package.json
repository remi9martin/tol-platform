{
  "name": "@tol/worker",
  "version": "0.0.0",
  "private": true,
  "description": "BullMQ/Redis job runner. Keeps expensive, retryable and time-based work out of request handlers (the spec): passport-readiness, capacity-freshness, rfq-expiry, economics-accrual this pass (earlier) — evidence-expiry, match-recompute, notification-dispatch, outbox-publish, audit-seal, connector-poll, analytics-rollup remain named, unbuilt gaps (see README.md).",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "start": "node dist/server.js",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src",
    "test": "vitest run"
  },
  "dependencies": {
    "@tol/authz": "workspace:*",
    "@tol/config": "workspace:*",
    "@tol/contracts": "workspace:*",
    "@tol/db": "workspace:*",
    "@tol/domain": "workspace:*",
    "@tol/events": "workspace:*",
    "@tol/evidence": "workspace:*",
    "@tol/queue": "workspace:*",
    "bullmq": "6.1.2",
    "fastify": "5.12.0",
    "ioredis": "5.11.1",
    "pino": "10.3.1"
  },
  "devDependencies": {
    "@types/node": "24.13.3",
    "tsx": "4.23.12",
    "typescript": "^5.9.3",
    "vitest": "^3.2.7"
  }
}
