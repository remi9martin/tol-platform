# infra/terraform — Production AWS Provisioning

Terraform-provisioned AWS infrastructure for production (ECS/Fargate, RDS Multi-AZ, ElastiCache, S3, KMS, Secrets Manager — the spec). Not part of the P0-P20 MVP gate set directly; deferred until the platform is ready to deploy. Status: empty. No deploy happens from this earlier pass — see the directive's explicit 'Do NOT deploy.'
