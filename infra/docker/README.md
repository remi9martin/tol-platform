# infra/docker — Local Dockerfiles

Dockerfiles for apps/api and apps/worker, referenced by the root docker-compose.yml once each app has real code to containerize. Status: empty — no Dockerfiles yet, nothing to build.
