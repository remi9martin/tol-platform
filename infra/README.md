# infra — Local + Production Infrastructure

Docker (local), Terraform (production AWS), and ops scripts. See infra/docker, infra/terraform, infra/scripts. Status: skeleton folders only, per the build log.
